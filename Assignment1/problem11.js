// Lowercase Uppercase

let c = "X";
if (c >= "A" && c <= "Z") {
    console.log("The Character is Uppercase")
} else {
    console.log("The Character is Lowercase")
}
// ternary
console.log((c >= "A" && c <= "Z") ? "Uppercase" : "Lowercase")