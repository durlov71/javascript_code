// Positive negative
//if else

if (number > 0) {
    console.log('Positive')
} else if (number === 0) {
    console.log('ZERO')
} else {
    console.log('Entered number is negative')
}

// ternary operators
console.log((number > 0) ? 'POSITIVE' : (number === 0) ? 'its ZERO' : 'Negative');